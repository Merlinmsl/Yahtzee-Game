// Online C compiler to run C program online
#include <stdio.h>

void diceReroll_com (Player *player, int N) {
    for (int i = N; i < 5; i++){
        player->dice[i] = (rand() % 6) + 1;
    }
}

int main() {
    if (frequencies[0] >= 3) {
        int x;
        do {
            diceReroll_com (&computer, 3);
            x++;
        }
        while (x < 2 || frequencies[0] >= 3);
    }
    if (frequencies[1] >= 3) {
        int x;
        do {
            diceReroll_com (&computer, 3);
            x++;
        }
        while (x < 2 || frequencies[0] >= 3);
    }        
    }
    if (frequencies[2] >= 3) {
        int x;
        do {
            diceReroll_com (&computer, 3);
            x++;
        }
        while (x < 2 || frequencies[0] >= 3);
    }        
    }
    if (frequencies[3] >= 3) {
         int x;
        do {
            diceReroll_com (&computer, 3);
            x++;
        }
        while (x < 2 || frequencies[0] >= 3);
    }       
    }
    if (frequencies[4] >= 3) {
         int x;
        do {
            diceReroll_com (&computer, 3);
            x++;
        }
        while (x < 2 || frequencies[0] >= 3);
    }       
    }
    if (frequencies[5] >= 3) {
        int x;
        do {
            diceReroll_com (&computer, 3);
            x++;
        }
        while (x < 2 || frequencies[0] >= 3);
    }        
    }
    if (frequencies[] >= 13) {
        int x;
        do {
            diceReroll_com (&computer, 3);
            x++;
        }
        while (x < 2 || frequencies[0] >= 3);
    }        
    }
    if (frequencies[] >= 13) {
        
    }
    if (frequencies[] >= 13) {
        
    }

    return 0;
}